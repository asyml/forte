from testbook import testbook


@testbook(
    "docs/notebook_tutorial/wrap_MT_inference_pipeline.ipynb", execute=False
)
def test_wrap_MT_inference_pipeline(tb):
    # if we just want to run through the notebook
    pass
